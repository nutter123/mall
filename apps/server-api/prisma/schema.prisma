generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model Address {
  id            BigInt    @id @default(autoincrement())
  userId        BigInt    @map("user_id")
  addressDetail String    @map("address_detail") @db.VarChar(255)
  province      String?   @db.VarChar(50)
  city          String?   @db.VarChar(50)
  district      String?   @db.VarChar(50)
  latitude      Decimal   @db.Decimal(10, 7)
  longitude     Decimal   @db.Decimal(11, 7)
  name          String    @db.VarChar(100)
  phone         String    @db.VarChar(20)
  gender        String?   @db.VarChar(10)
  label         String?   @db.VarChar(50)
  type          String?   @db.VarChar(50)
  typeCode      String?   @map("type_code") @db.VarChar(50)
  poipoistring  String?   @db.VarChar(255)
  useType       String?   @map("use_type") @db.VarChar(50)
  shopId        BigInt?   @map("shop_id")
  usageCount    Int?      @default(0) @map("usage_count")
  lastUsedDate  DateTime? @map("last_used_date") @db.DateTime(0)
  createdDate   DateTime  @default(now()) @map("created_date") @db.DateTime(0)
  remark        String?   @db.VarChar(255)
  poi           String?   @db.VarChar(255)
  inSite        Boolean?  @map("in_site")

  @@index([userId], map: "idx_user_id")
  @@map("address")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model Article {
  id          BigInt   @id @default(autoincrement())
  articleType String   @map("article_type") @db.VarChar(50)
  title       String   @db.VarChar(255)
  content     String?  @db.LongText
  sortOrder   Int?     @default(0) @map("sort_order")
  status      String?  @default("PUBLISHED") @db.VarChar(20)
  createdAt   DateTime @default(now()) @map("created_at") @db.DateTime(0)
  updatedAt   DateTime @default(now()) @map("updated_at") @db.DateTime(0)

  @@index([articleType, sortOrder], map: "idx_type_sort")
  @@map("article")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model Brand {
  id      BigInt  @id @default(autoincrement())
  name    String  @db.VarChar(100)
  logoUrl String? @map("logo_url") @db.VarChar(255)

  @@map("brand")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model Category {
  id              BigInt  @id @default(autoincrement())
  parentId        BigInt? @default(0) @map("parent_id")
  categoryName    String  @map("category_name") @db.VarChar(255)
  icon            String? @db.VarChar(255)
  backgroundColor String? @map("background_color") @db.VarChar(50)
  goodsNum        Int?    @default(0) @map("goods_num")
  menuType        String  @map("menu_type") @db.VarChar(50)
  promotionCode   String? @map("promotion_code") @db.VarChar(100)
  sort            Int?    @default(0)

  @@index([parentId], map: "idx_parent_id")
  @@map("category")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model CategoryAttributeLink {
  id          BigInt @id @default(autoincrement())
  categoryId  BigInt @map("category_id")
  attributeId BigInt @map("attribute_id")

  @@unique([categoryId, attributeId], map: "uk_category_attribute")
  @@map("category_attribute_link")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model CategoryBrandLink {
  id         BigInt @id @default(autoincrement())
  categoryId BigInt @map("category_id")
  brandId    BigInt @map("brand_id")

  @@unique([categoryId, brandId], map: "uk_category_brand")
  @@map("category_brand_link")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model CategoryFilterAttribute {
  id   BigInt @id @default(autoincrement())
  name String @unique(map: "uk_name") @db.VarChar(100)

  @@map("category_filter_attribute")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://prisly.d/database-comments
model CategoryFilterValue {
  id          BigInt @id @default(autoincrement())
  attributeId BigInt @map("attribute_id")
  valueName   String @map("value_name") @db.VarChar(100)

  @@index([attributeId], map: "idx_attribute_id")
  @@map("category_filter_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model CategoryProductRelation {
  id         BigInt    @id @default(autoincrement())
  categoryId BigInt    @map("category_id")
  productId  BigInt    @map("product_id")
  createTime DateTime? @default(now()) @map("create_time") @db.DateTime(0)
  updateTime DateTime? @default(now()) @map("update_time") @db.DateTime(0)

  @@unique([categoryId, productId], map: "uk_category_good")
  @@index([categoryId], map: "idx_category_id")
  @@index([productId], map: "idx_good_id")
  @@map("category_product_relation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ComboPackage {
  id                  BigInt   @id @default(autoincrement())
  name                String   @db.VarChar(255)
  title               String?  @db.VarChar(255)
  coverUrl            String?  @map("cover_url") @db.VarChar(255)
  price               Int
  minusAmount         Int?     @default(0) @map("minus_amount")
  isDisabled          Boolean? @default(false) @map("is_disabled")
  isHidden            Boolean? @default(false) @map("is_hidden")
  hideOnStockShortage Boolean? @default(false) @map("hide_on_stock_shortage")
  limitStockAll       Int?     @map("limit_stock_all")
  limitStockDay       Int?     @map("limit_stock_day")

  @@map("combo_package")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ComboPackageItem {
  id             BigInt @id @default(autoincrement())
  comboPackageId BigInt @map("combo_package_id")
  productSkuId   BigInt @map("product_sku_id")
  quantity       Int    @default(1)
  inListDisplay  Int?   @default(1) @map("in_list_display")
  sortOrder      Int?   @default(0) @map("sort_order")

  @@index([comboPackageId], map: "idx_combo_package_id")
  @@index([productSkuId], map: "idx_product_sku_id")
  @@map("combo_package_item")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model CouponTemplate {
  id              BigInt    @id @default(autoincrement())
  name            String    @db.VarChar(255)
  discountType    String    @map("discount_type") @db.VarChar(50)
  discountAmount  Int       @map("discount_amount")
  isLimitedAmount Boolean?  @default(false) @map("is_limited_amount")
  limitAmountMin  Int?      @default(0) @map("limit_amount_min")
  isComboCoupon   Boolean?  @default(false) @map("is_combo_coupon")
  validFrom       DateTime? @map("valid_from") @db.DateTime(0)
  validTo         DateTime? @map("valid_to") @db.DateTime(0)
  totalSupply     Int?      @default(-1) @map("total_supply")
  status          String?   @default("ACTIVE") @db.VarChar(20)

  @@map("coupon_template")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model Good {
  id                         BigInt    @id @default(autoincrement())
  backgroundColor            String?   @map("background_color") @db.VarChar(255)
  categoryName               String?   @map("category_name") @db.VarChar(255)
  collect                    Boolean?
  comboId                    BigInt?   @map("combo_id")
  comboPrice                 Int?      @map("combo_price")
  couponFlag                 Boolean?  @map("coupon_flag")
  estimateNorPrice           Int?      @map("estimate_nor_price")
  estimatePrice              Int?      @map("estimate_price")
  estimateType               String?   @map("estimate_type") @db.VarChar(255)
  estimateVipPrice           Int?      @map("estimate_vip_price")
  frozen                     Boolean?
  fullGiftLabel              String?   @map("full_gift_label") @db.VarChar(255)
  goods1LvlCatId             BigInt?   @map("goods1_lvl_cat_id")
  goods1LvlCatName           String?   @map("goods1_lvl_cat_name") @db.VarChar(255)
  goods2LvlCatId             BigInt?   @map("goods2_lvl_cat_id")
  goods2LvlCatName           String?   @map("goods2_lvl_cat_name") @db.VarChar(255)
  goodsCashBack              Boolean?  @map("goods_cash_back")
  groupName                  String?   @map("group_name") @db.VarChar(255)
  imgUrl                     String?   @map("img_url") @db.VarChar(255)
  imgUrlNotGif               String?   @map("img_url_not_gif") @db.VarChar(255)
  intellectSuggestId         String?   @map("intellect_suggest_id") @db.VarChar(255)
  intellectSuggestName       String?   @map("intellect_suggest_name") @db.VarChar(255)
  intellectSuggestRelation   String?   @map("intellect_suggest_relation") @db.VarChar(255)
  labelImg                   String?   @map("label_img") @db.VarChar(255)
  limitOnceMax               Int?      @map("limit_once_max")
  limitOnceMaxOn             Boolean?  @map("limit_once_max_on")
  limitOnceMin               Int?      @map("limit_once_min")
  limitOnceMinOn             Boolean?  @map("limit_once_min_on")
  listLabel                  String?   @map("list_label") @db.VarChar(255)
  memberDayLimit             Int?      @map("member_day_limit")
  memberDayTimes             Int?      @map("member_day_times")
  minSpecPrice               Int?      @map("min_spec_price")
  minUnitName                String?   @map("min_unit_name") @db.VarChar(255)
  onlyEatIn                  Boolean?  @map("only_eat_in")
  original                   Boolean?
  price                      Int?
  rankingName                String?   @map("ranking_name") @db.VarChar(255)
  rankingNum                 Int?      @map("ranking_num")
  rankingType                Int?      @map("ranking_type")
  recommendSort              Int?      @map("recommend_sort")
  saleId                     BigInt?   @map("sale_id")
  saleName                   String?   @map("sale_name") @db.VarChar(255)
  saleType                   Int?      @map("sale_type")
  sellPoint                  String?   @map("sell_point") @db.VarChar(255)
  sensorsRecommendItemMsgDto Json?     @map("sensors_recommend_item_msg_dto") @db.Json
  shopStock                  Int?      @map("shop_stock")
  sort                       Int?
  specMore                   Boolean?  @map("spec_more")
  specName                   String?   @map("spec_name") @db.VarChar(255)
  specStock                  Int?      @map("spec_stock")
  speedDelivery              Boolean?  @map("speed_delivery")
  spuId                      BigInt?   @map("spu_id")
  status                     Int?
  times                      Int?
  timesTime                  DateTime? @map("times_time") @db.DateTime(0)
  userVip                    Boolean?  @map("user_vip")
  userVipPrice               Boolean?  @map("user_vip_price")
  videoImg                   String?   @map("video_img") @db.VarChar(255)
  videoUrl                   String?   @map("video_url") @db.VarChar(255)
  vipPrice                   Int?      @map("vip_price")
  vipPriceSts                Boolean?  @map("vip_price_sts")
  wineCoefficient            Float?    @map("wine_coefficient")

  @@map("good")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model InventoryStock {
  id            BigInt @id @default(autoincrement())
  skuId         BigInt @map("sku_id")
  locationId    BigInt @map("location_id")
  locationType  String @map("location_type") @db.VarChar(20)
  stockQuantity Int?   @default(0) @map("stock_quantity")

  @@unique([skuId, locationId, locationType], map: "uk_sku_location")
  @@index([locationId, locationType], map: "idx_location")
  @@map("inventory_stock")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model Navigation {
  id                 BigInt  @id @default(autoincrement())
  backColor          String? @map("back_color") @db.VarChar(255)
  backToTopIcon      String? @map("back_to_top_icon") @db.VarChar(255)
  backToTopName      String? @map("back_to_top_name") @db.VarChar(255)
  backToTopNameColor String? @map("back_to_top_name_color") @db.VarChar(255)
  highlightIcon      String? @map("highlight_icon") @db.VarChar(255)
  highlightNameColor String? @map("highlight_name_color") @db.VarChar(255)
  icon               String? @db.VarChar(255)
  linkName           String? @map("link_name") @db.VarChar(255)
  linkType           String? @map("link_type") @db.VarChar(255)
  linkValue          String? @map("link_value") @db.VarChar(255)
  name               String? @db.VarChar(255)
  nameColor          String? @map("name_color") @db.VarChar(255)
  sort               Int?
  type               String? @db.VarChar(255)
  slogan             String? @db.VarChar(255)

  @@map("navigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model Order {
  id                           BigInt    @id @default(autoincrement())
  orderType                    Int       @map("order_type") @db.TinyInt
  orderNo                      String    @map("order_no") @db.VarChar(50)
  orderChannelNo               String?   @map("order_channel_no") @db.VarChar(50)
  siteId                       BigInt    @map("site_id")
  siteName                     String?   @map("site_name") @db.VarChar(100)
  shopName                     String?   @map("shop_name") @db.VarChar(100)
  shopLocation                 String?   @map("shop_location") @db.VarChar(50)
  totalMoney                   Int       @map("total_money")
  discountMoney                Int       @map("discount_money")
  payMoney                     Int       @map("pay_money")
  payStatus                    Int?      @map("pay_status") @db.TinyInt
  payWay                       Int?      @map("pay_way") @db.TinyInt
  mallStatus                   Int?      @map("mall_status") @db.TinyInt
  status                       Int?      @db.TinyInt
  bespokeTime                  DateTime? @map("bespoke_time") @db.DateTime(0)
  orderCreateTime              DateTime  @default(now()) @map("order_create_time") @db.DateTime(0)
  orderPayTime                 DateTime? @map("order_pay_time") @db.DateTime(0)
  orderPayLimitTime            Int?      @map("order_pay_limit_time")
  orderPayEndTime              DateTime? @map("order_pay_end_time") @db.DateTime(0)
  produceType                  Int?      @map("produce_type") @db.TinyInt
  cashOnDelivery               Boolean?  @default(false) @map("cash_on_delivery")
  lonLat                       String?   @map("lon_lat") @db.VarChar(50)
  isSvipSite                   Boolean?  @default(false) @map("is_svip_site")
  isApplyInvoice               Boolean?  @default(false) @map("is_apply_invoice")
  provideSpecialTicke          Boolean?  @default(false) @map("provide_special_ticke")
  settlementOrderNo            String?   @map("settlement_order_no") @db.VarChar(50)
  settlementStatus             Int?      @map("settlement_status") @db.TinyInt
  businessType                 Int?      @map("business_type") @db.TinyInt
  orderIsModify                Boolean?  @default(false) @map("order_is_modify")
  orderGoodsQuantity           Int?      @default(0) @map("order_goods_quantity")
  userId                       BigInt?   @map("user_id")
  handleStatus                 Int?      @map("handle_status")
  orderDistributeTime          DateTime? @map("order_distribute_time") @db.DateTime(0)
  orderAcceptTime              DateTime? @map("order_accept_time") @db.DateTime(0)
  orderDeliveryTime            DateTime? @map("order_delivery_time") @db.DateTime(0)
  orderFinishTime              DateTime? @map("order_finish_time") @db.DateTime(0)
  produceOrg                   Int?      @map("produce_org")
  reminderNum                  Int?      @map("reminder_num")
  reminderStatus               Boolean?  @map("reminder_status")
  invoiceStatus                Int?      @map("invoice_status")
  limitAfterSales              Boolean?  @map("limit_after_sales")
  orderEvaluate                Boolean?  @map("order_evaluate")
  orderDeliveryEvaluate        Boolean?  @map("order_delivery_evaluate")
  overtimePaymentApproveStatus Boolean?  @default(false) @map("overtime_payment_approve_status")
  noOvertimePaymentType        Boolean?  @default(false) @map("no_overtime_payment_type")
  isReplenishInvoice           Boolean?  @default(false) @map("is_replenish_invoice")
  description                  String?   @db.VarChar(255)
  tableNo                      String?   @map("table_no") @db.VarChar(50)
  tablePeople                  String?   @map("table_people") @db.VarChar(50)
  speedDeliveryId              String?   @map("speed_delivery_id") @db.VarChar(50)
  spilt                        String?   @db.VarChar(50)
  receiptCode                  String?   @map("receipt_code") @db.VarChar(50)
  receiptDetail                String?   @map("receipt_detail") @db.VarChar(50)
  highValueUserType            String?   @map("high_value_user_type") @db.VarChar(50)
  payMoneyNotVirtual           Int       @map("pay_money_not_virtual")
  orderGoodsQuantityNotVirtual Int       @map("order_goods_quantity_not_virtual")
  integralNum                  Int       @map("integral_num")
  traceId                      String?   @map("trace_id") @db.VarChar(50)

  @@index([orderNo], map: "idx_order_no")
  @@map("order")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model OrderCancel {
  orderId        BigInt    @id @default(autoincrement()) @map("order_id")
  stage          Int?      @db.TinyInt
  type           Int?      @db.TinyInt
  userCancelType Int?      @map("user_cancel_type") @db.TinyInt
  status         Int?      @db.TinyInt
  reason         String?   @db.VarChar(255)
  remarks        String?   @db.VarChar(255)
  applyTime      DateTime? @map("apply_time") @db.DateTime(0)
  cancelTime     DateTime? @map("cancel_time") @db.DateTime(0)

  @@map("order_cancel")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model OrderDelivery {
  orderId          BigInt   @id @default(autoincrement()) @map("order_id")
  deliveryUserId   BigInt?  @map("delivery_user_id")
  deliveryName     String?  @map("delivery_name") @db.VarChar(100)
  deliveryWay      Int?     @map("delivery_way") @db.TinyInt
  deliveryType     Int?     @map("delivery_type") @db.TinyInt
  deliveryStatus   Int?     @map("delivery_status") @db.TinyInt
  deliveryText     String?  @map("delivery_text") @db.VarChar(255)
  deliveryDistance Int?     @map("delivery_distance")
  distance         Int?
  deliveryPosition String?  @map("delivery_position") @db.VarChar(50)
  deliveryImg      String?  @map("delivery_img") @db.VarChar(255)
  deliveryRemark   String?  @map("delivery_remark") @db.VarChar(255)
  createdAt        DateTime @default(now()) @map("created_at") @db.DateTime(0)
  updatedAt        DateTime @default(now()) @map("updated_at") @db.DateTime(0)

  @@index([deliveryUserId], map: "idx_delivery_user_id")
  @@map("order_delivery")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model OrderGoods {
  id                BigInt   @id @default(autoincrement())
  orderId           BigInt   @map("order_id")
  goodsId           BigInt   @map("goods_id")
  spuId             BigInt   @map("spu_id")
  saleName          String   @map("sale_name") @db.VarChar(255)
  goodsQuantity     Int      @map("goods_quantity")
  price             Int
  payUnitMoney      Int      @map("pay_unit_money")
  saleSpecName      String?  @map("sale_spec_name") @db.VarChar(255)
  specNum           Int?     @map("spec_num")
  imgUrl            String?  @map("img_url") @db.VarChar(255)
  imgUrlNotGif      String?  @map("img_url_not_gif") @db.VarChar(255)
  firstCategoryId   BigInt?  @map("first_category_id")
  firstCategoryName String?  @map("first_category_name") @db.VarChar(100)
  onlyEatIn         Boolean? @default(false) @map("only_eat_in")
  authentic         Boolean? @default(false)
  speedDelivery     Boolean? @default(false) @map("speed_delivery")
  overtimePayment   Boolean? @default(false) @map("overtime_payment")
  cashOnDelivery    Boolean? @default(false) @map("cash_on_delivery")
  frozen            Boolean? @default(false)
  allowBuy          Boolean? @default(true) @map("allow_buy")
  useCoupon         Boolean? @default(false) @map("use_coupon")
  createdAt         DateTime @default(now()) @map("created_at") @db.DateTime(0)
  status            Int?
  inList            Int?     @map("in_list")
  specName          String?  @map("spec_name") @db.VarChar(255)
  gift              Int?     @db.TinyInt
  addMoneyBuy       Int?     @map("add_money_buy") @db.TinyInt

  @@index([goodsId], map: "idx_goods_id")
  @@index([orderId], map: "idx_order_id")
  @@map("order_goods")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model OrderGoodsSplit {
  id            BigInt @id @default(autoincrement())
  orderGoodsId  BigInt @map("order_goods_id")
  goodsQuantity Int    @map("goods_quantity")
  priceType     String @map("price_type") @db.VarChar(50)
  nowPrice      Int    @map("now_price")
  price         Int
  integral      Int?   @default(0)
  priceSort     Int?   @default(0) @map("price_sort")

  @@index([orderGoodsId], map: "idx_order_goods_id")
  @@map("order_goods_split")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model OrderInvoice {
  id                 BigInt @id @default(autoincrement())
  orderId            BigInt @map("order_id")
  invoiceType        Int    @map("invoice_type") @db.TinyInt
  invoiceSpecialType Int    @map("invoice_special_type") @db.TinyInt
  invoiceStatus      Int    @map("invoice_status") @db.TinyInt

  @@map("order_invoice")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model OrderPrize {
  id               BigInt   @id @default(autoincrement())
  prizeType        String?  @map("prize_type") @db.VarChar(50)
  prizeDescription String?  @map("prize_description") @db.VarChar(255)
  notRecyclePrize  Boolean? @default(false) @map("not_recycle_prize")
  evaluatePrize    Boolean? @default(false) @map("evaluate_prize")

  @@map("order_prize")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model PictureAdAreas {
  id        BigInt   @id @default(autoincrement()) @map("id") @db.UnsignedBigInt
  adId      BigInt   @map("ad_id") @db.UnsignedBigInt
  name      String   @db.VarChar(255)
  area      String   @db.VarChar(100)
  linkType  String   @map("link_type") @db.VarChar(100)
  linkValue String   @map("link_value") @db.VarChar(255)
  areaRem   String?  @map("area_rem") @db.VarChar(255)
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamp(0)

  @@index([adId], map: "idx_ad_id")
  @@map("picture_ad_areas")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model PictureAds {
  id        BigInt   @id @default(autoincrement()) @map("id") @db.UnsignedBigInt
  name      String   @db.VarChar(255)
  pic       String   @db.VarChar(1024)
  status    Boolean  @default(true)
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamp(0)

  @@map("picture_ads")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ProductAttribute {
  id             BigInt @id @default(autoincrement())
  spuId          BigInt @map("spu_id")
  attributeName  String @map("attribute_name") @db.VarChar(100)
  attributeValue String @map("attribute_value") @db.VarChar(255)

  @@index([spuId], map: "idx_spu_id")
  @@map("product_attribute")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ProductImage {
  id        BigInt @id @default(autoincrement())
  skuId     BigInt @map("sku_id")
  imgUrl    String @map("img_url") @db.VarChar(255)
  sortOrder Int?   @default(0) @map("sort_order")

  @@index([skuId], map: "idx_spu_id")
  @@map("product_image")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ProductOtherService {
  id           BigInt @id @default(autoincrement())
  spuId        BigInt @map("spu_id")
  serviceCode  String @map("service_code") @db.VarChar(50)
  serviceLabel String @map("service_label") @db.VarChar(100)

  @@index([spuId], map: "idx_spu_id")
  @@map("product_other_service")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ProductSku {
  id                    BigInt   @id @default(autoincrement())
  spuId                 BigInt   @map("spu_id")
  siteId                BigInt?  @map("site_id")
  specName              String?  @map("spec_name") @db.VarChar(255)
  price                 Int?
  vipPrice              Int?     @map("vip_price")
  specPrice             Int?     @map("spec_price")
  original              Boolean? @default(false)
  vipPriceSts           Boolean? @default(false) @map("vip_price_sts")
  shopStock             Int?     @map("shop_stock")
  specStock             Int?     @map("spec_stock")
  minUnitName           String?  @map("min_unit_name") @db.VarChar(255)
  limitOnceMax          Int?     @map("limit_once_max")
  limitOnceMaxOn        Boolean? @default(false) @map("limit_once_max_on")
  limitOnceMin          Int?     @map("limit_once_min")
  limitOnceMinOn        Boolean? @default(false) @map("limit_once_min_on")
  memberDayLimit        Int?     @map("member_day_limit")
  memberDayTimes        Int?     @map("member_day_times")
  limitAllQuantity      Int?     @default(2147483647) @map("limit_all_quantity")
  limitDayQuantity      Int?     @default(2147483647) @map("limit_day_quantity")
  specNum               Int?     @map("spec_num")
  authentic             Boolean?
  overtimePayment       Boolean? @map("overtime_payment")
  cashOnDelivery        Boolean? @map("cash_on_delivery")
  limitStockAllQuantity Int?     @default(2147483647) @map("limit_stock_all_quantity")
  siteStock             Int?     @map("site_stock")
  defaultImgId          BigInt?  @map("default_img_id")
  status                Int?
  allowBuy              Boolean? @default(true) @map("allow_buy")
  min                   Boolean?
  limitAll              Int?     @default(2147483647) @map("limit_all")
  limitAllMax           Int?     @default(2147483647) @map("limit_all_max")

  @@index([spuId], map: "idx_spu_id")
  @@map("product_sku")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ProductSpecKey {
  id       BigInt @id @default(autoincrement())
  spuId    BigInt @map("spu_id")
  specName String @map("spec_name") @db.VarChar(100)

  @@index([spuId], map: "idx_spu_id")
  @@map("product_spec_key")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ProductSpecValue {
  id        BigInt @id @default(autoincrement())
  specKeyId BigInt @map("spec_key_id")
  specValue String @map("spec_value") @db.VarChar(100)

  @@index([specKeyId], map: "idx_key_id")
  @@map("product_spec_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ProductSpu {
  id                 BigInt   @id @default(autoincrement())
  saleName           String?  @map("sale_name") @db.VarChar(255)
  sellPoint          String?  @map("sell_point") @db.VarChar(255)
  categoryId         BigInt?  @map("category_id")
  groupName          String?  @map("group_name") @db.VarChar(255)
  mainImageUrl       String?  @map("main_image_url") @db.VarChar(255)
  mainImageUrlNotGif String?  @map("main_image_url_not_gif") @db.VarChar(255)
  videoUrl           String?  @map("video_url") @db.VarChar(255)
  videoImg           String?  @map("video_img") @db.VarChar(255)
  backgroundColor    String?  @map("background_color") @db.VarChar(255)
  listLabel          String?  @map("list_label") @db.VarChar(255)
  labelImage         String?  @map("label_image") @db.VarChar(255)
  saleType           Int?     @map("sale_type")
  onlyEatIn          Boolean? @default(false) @map("only_eat_in")
  frozen             Boolean? @default(false)
  speedDelivery      Boolean? @default(false) @map("speed_delivery")
  goodsCashBack      Boolean? @default(false) @map("goods_cash_back")
  wineCoefficient    Float?   @map("wine_coefficient")
  specMore           Boolean? @default(false) @map("spec_more")
  status             Int?
  sort               Int?     @default(0)
  recommendSort      Int?     @default(0) @map("recommend_sort")
  details            String?  @db.Text
  authentic          Boolean? @default(false)
  overtimePayment    Boolean? @default(false) @map("overtime_payment")
  cashOnDelivery     Boolean? @default(false) @map("cash_on_delivery")
  aloneSend          Boolean? @default(false) @map("alone_send")
  shareTitle         String?  @map("share_title") @db.VarChar(255)
  shareInfo          String?  @map("share_info") @db.VarChar(255)
  shareImg           String?  @map("share_img") @db.VarChar(255)
  sharingImage       String?  @map("sharing_image") @db.VarChar(255)
  titleMain          String?  @map("title_main") @db.VarChar(255)
  brandId            BigInt?  @map("brand_id")
  evaluateNums       Int?     @default(0) @map("evaluate_nums")
  rulesExplain       String?  @map("rules_explain") @db.Text
  saleId             BigInt?  @map("sale_id")
  saleSpecName       String?  @map("sale_spec_name") @db.VarChar(20)
  defaultSkuId       BigInt?  @map("default_sku_id")

  @@map("product_spu")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model PromotionActivity {
  id               BigInt   @id @default(autoincrement())
  name             String   @db.VarChar(255)
  promotionCode    String?  @map("promotion_code") @db.VarChar(100)
  sortOrder        Int?     @default(0) @map("sort_order")
  targetCategoryId BigInt?  @map("target_category_id")
  activityName     String   @map("activity_name") @db.VarChar(255)
  activityType     String   @map("activity_type") @db.VarChar(50)
  label            String?  @db.VarChar(100)
  startTime        DateTime @map("start_time") @db.DateTime(0)
  endTime          DateTime @map("end_time") @db.DateTime(0)
  status           String?  @db.VarChar(50)

  @@map("promotion_activity")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model PromotionProductLink {
  id           BigInt @id @default(autoincrement())
  activityId   BigInt @map("activity_id")
  productSpuId BigInt @map("product_spu_id")
  sortOrder    Int?   @default(0) @map("sort_order")

  @@index([activityId], map: "idx_activity_id")
  @@index([productSpuId], map: "idx_product_spu_id")
  @@map("promotion_product_link")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model PromotionSkuLink {
  id             BigInt @id @default(autoincrement())
  activityId     BigInt @map("activity_id")
  skuId          BigInt @map("sku_id")
  promotionPrice Int?   @map("promotion_price")

  @@index([activityId], map: "idx_activity_id")
  @@index([skuId], map: "idx_sku_id")
  @@map("promotion_sku_link")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model RecommendZone {
  id              BigInt   @id @default(autoincrement())
  name            String   @db.VarChar(255)
  namePic         String?  @map("name_pic") @db.VarChar(255)
  nameType        String?  @default("text") @map("name_type") @db.VarChar(50)
  subhead         String?  @db.VarChar(255)
  type            String   @db.VarChar(50)
  showNum         Int?     @default(0) @map("show_num")
  linkType        String?  @map("link_type") @db.VarChar(50)
  linkValue       String?  @map("link_value") @db.VarChar(255)
  linkName        String?  @map("link_name") @db.VarChar(100)
  display         Boolean  @default(true)
  sort            Int?     @default(0)
  orderCategoryId BigInt?  @map("order_category_id")
  haveSensors     Boolean  @default(false) @map("have_sensors")
  createdAt       DateTime @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt       DateTime @default(now()) @map("updated_at") @db.Timestamp(0)

  @@index([sort, display], map: "idx_sort_display")
  @@map("recommend_zone")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model Site {
  id                         BigInt    @id @default(autoincrement())
  name                       String    @db.VarChar(100)
  city                       String?   @db.VarChar(50)
  pinyin                     String?   @db.VarChar(100)
  pinyinInitial              String?   @map("pinyin_initial") @db.VarChar(10)
  latitude                   Decimal   @db.Decimal(10, 7)
  longitude                  Decimal   @db.Decimal(11, 7)
  status                     Int?      @default(0)
  expectedOpenTime           DateTime? @map("expected_open_time") @db.Date
  primaryShopId              BigInt?   @map("primary_shop_id")
  businessStartTime          String?   @map("business_start_time") @db.VarChar(10)
  businessEndTime            String?   @map("business_end_time") @db.VarChar(10)
  deliveryTimeMinutes        Int?      @map("delivery_time_minutes")
  premiumDeliveryTimeMinutes Int?      @map("premium_delivery_time_minutes")
  invoiceType                String?   @map("invoice_type") @db.VarChar(50)
  provideSpecialTicket       Boolean?  @default(false) @map("provide_special_ticket")
  enableReservation          Boolean?  @default(false) @map("enable_reservation")
  reservationStartTime       String?   @map("reservation_start_time") @db.VarChar(20)
  enableGodSite              Boolean?  @default(false) @map("enable_god_site")
  enableSvipSite             Boolean?  @default(false) @map("enable_svip_site")
  enableFastDelivery         Boolean?  @default(false) @map("enable_fast_delivery")
  enableRetailDist           Boolean?  @default(false) @map("enable_retail_dist")
  enableSiteInvitation       Boolean?  @default(false) @map("enable_site_invitation")
  hasSiteInviteTask          Boolean?  @default(false) @map("has_site_invite_task")
  allowInvitationCashOut     Boolean?  @default(false) @map("allow_invitation_cash_out")
  shopQuantity               Int?      @default(0) @map("shop_quantity")
  centerLocationCoords       String?   @map("center_location_coords") @db.VarChar(50)
  useMourningTheme           Boolean?  @default(false) @map("use_mourning_theme")
  showDeliveryArea           Boolean?  @default(false) @map("show_delivery_area")
  enableTransferActivity     Boolean?  @default(false) @map("enable_transfer_activity")
  version                    Int?      @default(1)

  @@map("site")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model SiteNotice {
  id        BigInt   @id @default(autoincrement())
  siteId    BigInt   @map("site_id")
  content   String   @db.VarChar(255)
  linkName  String?  @map("link_name") @db.VarChar(50)
  linkType  String?  @map("link_type") @db.VarChar(50)
  linkValue String?  @map("link_value") @db.VarChar(255)
  sortOrder Int?     @default(0) @map("sort_order")
  isActive  Boolean? @default(true) @map("is_active")

  @@index([siteId], map: "idx_site_id")
  @@map("site_notice")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model SiteNoticeInfo {
  id         BigInt    @id @default(autoincrement())
  siteId     BigInt    @map("site_id")
  title      String?   @db.VarChar(255)
  content    String?   @db.Text
  createTime DateTime? @default(now()) @map("create_time") @db.DateTime(0)

  @@index([siteId], map: "idx_site_id")
  @@map("site_notice_info")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model SiteWecomEntrance {
  id               BigInt    @id @default(autoincrement())
  siteId           BigInt    @unique(map: "uk_site_id") @map("site_id")
  isOpen           Boolean?  @default(false) @map("is_open")
  showArea         String?   @map("show_area") @db.VarChar(100)
  activityDescribe String?   @map("activity_describe") @db.VarChar(100)
  customDescribe   String?   @map("custom_describe") @db.VarChar(255)
  rule             String?   @db.Text
  htmlUrl          String?   @map("html_url") @db.VarChar(255)
  miniUrl          String?   @map("mini_url") @db.VarChar(255)
  qrCodeUrl        String?   @map("qr_code_url") @db.VarChar(255)
  shareTitle       String?   @map("share_title") @db.VarChar(100)
  sharePicUrl      String?   @map("share_pic_url") @db.VarChar(255)
  remark           String?   @db.Text
  createdAt        DateTime? @map("created_at") @db.DateTime(0)
  createdBy        String?   @map("created_by") @db.VarChar(50)
  updatedAt        DateTime? @map("updated_at") @db.DateTime(0)
  updatedBy        String?   @map("updated_by") @db.VarChar(50)

  @@map("site_wecom_entrance")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model User {
  id                 BigInt    @id @default(autoincrement())
  birthday           String?   @db.VarChar(20)
  experience         Int?
  gender             String?   @db.VarChar(10)
  hasPassword        Boolean?  @map("has_password")
  password           String?   @db.VarChar(255)
  hasPayPassword     Boolean?  @map("has_pay_password")
  payPassword        String?   @map("pay_password") @db.VarChar(255)
  icon               String?   @db.VarChar(255)
  integral           Int?
  isSvip             Boolean?  @map("is_svip")
  levelId            BigInt?   @map("level_id")
  levelName          String?   @map("level_name") @db.VarChar(50)
  nickName           String?   @map("nick_name") @db.VarChar(50)
  newUser            Boolean?  @map("new_user")
  oftenCity          String?   @map("often_city") @db.VarChar(50)
  payPasswordDisplay Boolean?  @map("pay_password_display")
  picture            String?   @db.VarChar(255)
  retailer           Boolean?
  tianCai            Boolean?  @map("tian_cai")
  totalAmount        Int?      @map("total_amount")
  userName           String?   @map("user_name") @db.VarChar(50)
  phone              String?   @db.VarChar(20)
  openId             String?   @unique @map("open_id") @db.VarChar(100) // 确保唯一性约束
  createTime         DateTime? @default(now()) @map("create_time")
  updateTime         DateTime? @updatedAt @map("update_time")
  godUser            Boolean?  @default(false) @map("god_user")

  @@map("user")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model UserCoupon {
  id               BigInt    @id @default(autoincrement())
  userId           BigInt    @map("user_id")
  couponTemplateId BigInt    @map("coupon_template_id")
  status           String    @default("UNUSED") @db.VarChar(20)
  receivedTime     DateTime  @default(now()) @map("received_time") @db.DateTime(0)
  usedTime         DateTime? @map("used_time") @db.DateTime(0)

  @@index([userId, status], map: "idx_user_status")
  @@map("user_coupon")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model UserOrder {
  id                     BigInt    @id @default(autoincrement())
  activityId             BigInt?   @map("activity_id")
  bespokeTime            DateTime? @map("bespoke_time") @db.DateTime(0)
  cashbackCount          Int?      @map("cashback_count")
  codeAging              Int?      @map("code_aging")
  deliveryText           String?   @map("delivery_text") @db.VarChar(255)
  haveCoupon             Boolean?  @map("have_coupon")
  haveIntegral           Boolean?  @map("have_integral")
  highValueUserType      Int?      @map("high_value_user_type")
  minImgUrl              String?   @map("min_img_url") @db.VarChar(255)
  orderCreateTime        DateTime? @map("order_create_time") @db.DateTime(0)
  orderExceed            Int?      @map("order_exceed")
  orderNo                String?   @map("order_no") @db.VarChar(255)
  orderScanCash          Boolean?  @map("order_scan_cash")
  orderType              Int?      @map("order_type")
  payMoney               Int?      @map("pay_money")
  payStatus              Int?      @map("pay_status")
  payType                String?   @map("pay_type") @db.VarChar(255)
  payWay                 Int?      @map("pay_way")
  prizeType              String?   @map("prize_type") @db.VarChar(255)
  receiptCode            String?   @map("receipt_code") @db.VarChar(255)
  scanRewardNum          Int?      @map("scan_reward_num")
  secondOrderGiftTotal   Int?      @map("second_order_gift_total")
  siteId                 BigInt?   @map("site_id")
  svipSite               Boolean?  @map("svip_site")
  svipUser               Boolean?  @map("svip_user")
  threeOrderGiftSum      Int?      @map("three_order_gift_sum")
  threeOrderGiftTotal    Int?      @map("three_order_gift_total")
  threeOrderIntroduction String?   @map("three_order_introduction") @db.VarChar(255)
  traceId                String?   @map("trace_id") @db.VarChar(255)

  @@map("user_order")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model UserProductInteraction {
  id               BigInt    @id @default(autoincrement())
  userId           BigInt    @map("user_id")
  spuId            BigInt    @map("spu_id")
  hasCollected     Boolean?  @default(false) @map("has_collected")
  purchaseTimes    Int?      @default(0) @map("purchase_times")
  lastPurchaseTime DateTime? @map("last_purchase_time") @db.DateTime(0)
  hasSubscribed    Boolean?  @default(false) @map("has_subscribed")
  subscribeId      BigInt?   @map("subscribe_id")

  @@unique([userId, spuId], map: "uk_user_spu")
  @@map("user_product_interaction")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model WeComEntrance {
  id                                BigInt    @id @default(autoincrement())
  siteId                            BigInt?   @map("site_id")
  activityDescribe                  String?   @map("activity_describe") @db.VarChar(255)
  createdBy                         String?   @map("created_by") @db.VarChar(50)
  createdDate                       DateTime? @map("created_date") @db.DateTime(0)
  createdDateEnd                    DateTime? @map("created_date_end") @db.DateTime(0)
  createdDateStart                  DateTime? @map("created_date_start") @db.DateTime(0)
  createdName                       String?   @map("created_name") @db.VarChar(100)
  customDescribe                    String?   @map("custom_describe") @db.Text
  goodsDetailCustomDescribe         String?   @map("goods_detail_custom_describe") @db.Text
  goodsDetailDescribe               String?   @map("goods_detail_describe") @db.VarChar(255)
  goodsDetailDiscountCustomDescribe String?   @map("goods_detail_discount_custom_describe") @db.Text
  goodsDetailDiscountDescribe       String?   @map("goods_detail_discount_describe") @db.VarChar(255)
  htmlUrl                           String?   @map("html_url") @db.VarChar(500)
  lastModifiedBy                    String?   @map("last_modified_by") @db.VarChar(50)
  lastModifiedDate                  DateTime? @map("last_modified_date") @db.DateTime(0)
  lastModifiedDateEnd               DateTime? @map("last_modified_date_end") @db.DateTime(0)
  lastModifiedDateStart             DateTime? @map("last_modified_date_start") @db.DateTime(0)
  lastModifiedName                  String?   @map("last_modified_name") @db.VarChar(100)
  miniUrl                           String?   @map("mini_url") @db.VarChar(500)
  open                              Boolean?  @default(false)
  personalDescribe                  String?   @map("personal_describe") @db.VarChar(255)
  proBg                             String?   @map("pro_bg") @db.VarChar(500)
  qrCode                            String?   @map("qr_code") @db.VarChar(500)
  remark                            String?   @db.Text
  rule                              String?   @db.Text
  sharePic                          String?   @map("share_pic") @db.VarChar(500)
  shareTitle                        String?   @map("share_title") @db.VarChar(255)
  showArea                          String?   @map("show_area") @db.VarChar(100)
  weChatShortUrl                    String?   @map("we_chat_short_url") @db.VarChar(500)
  dataPermission                    String?   @map("data_permission") @db.Text
  createTime                        DateTime? @default(now()) @map("create_time") @db.DateTime(0)
  updateTime                        DateTime? @default(now()) @map("update_time") @db.DateTime(0)

  @@index([createdDate], map: "idx_created_date")
  @@index([open], map: "idx_open")
  @@index([siteId], map: "idx_site_id")
  @@map("we_com_entrance")
}
