<view class="mall-search-container">
  <!-- 使用 TDesign 原生组件，但包裹在我们的容器里 -->
  <t-search 
    placeholder="{{placeholder}}" 
    value="{{value}}"
    bind:change="onChange"
    bind:submit="onSubmit"
    t-class="custom-t-search" 
  />
</view>