{
  "name": "mall-monorepo",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter server-api start:dev",
    "dev:admin": "pnpm --filter admin-web start",
    "openapi:gen": "pnpm --filter admin-web openapi",
    "dev": "turbo run dev:server dev:admin"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "turbo": "^1.10.0",
    "prettier":"3.7.3"
  }
}
