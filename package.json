{
  "name": "mall-monorepo",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter server-api start:dev",
    "dev:admin": "pnpm --filter admin-web start",
    "dev:client": "pnpm --filter client-mp dev",
    "dev": "turbo run dev:server dev:admin"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "turbo": "^1.10.0",
    "prettier":"3.7.3"
  }
}
