{
  "name": "mall-monorepo",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter server-api start:dev",
    "dev:admin": "pnpm --filter admin-web start",
    "dev:mp": "pnpm --filter client-mp dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "turbo": "^1.10.0"
  }
}